/**
 * AuthMethod
 *
 * @group auth-method
 * @group unit
 */

describe("AuthMethod", () => {
  describe("instance", () => {
    it.todo("should have context property");
  });

  describe("get credentials()", () => {
    it.todo("should return _credentials if set");
    it.todo("should throw error if _credentials not set");
  });

  describe("set credentials()", () => {
    it.todo("should set _credentials if not set");
    it.todo("should throw error if _credentials already set");
  });

  describe("setCredentials()", () => {
    it.todo("should call credentials setter");
  });
});
