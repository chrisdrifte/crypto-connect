/**
 * serializeRequestBody
 *
 * @group utility
 * @group unit
 */

describe("serializeRequestBody", () => {
  it.todo("should return parameters string by default");
  it.todo(
    "should return parameters string (application/x-www-form-urlencoded)",
  );
  it.todo("should return json string (application/json)");
  it.todo("should throw error when (xxx/yyy)");
});
